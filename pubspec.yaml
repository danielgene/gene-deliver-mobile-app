name: gene_deliver
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.2
  fluttertoast: ^7.1.1
  getwidget: ^1.2.2
  shared_preferences: ^0.5.12+4
  intl: ^0.16.1
  pull_to_refresh: ^1.6.2
  future_progress_dialog: ^0.1.1
  syncfusion_flutter_signaturepad: ^18.3.50-beta
  flutter_cache_manager: ^1.1.3

  geolocator: ^6.1.7
  dio: ^3.0.10
  modal_progress_hud: ^0.1.3
  path_provider: ^1.6.24
  geocoder: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
